# SB-Microsoft-Learn-Ideaagent-v1

Auto-generated by the **Idea Generator Agent**.

- **Technique**: rag
- **Environment**: dev
- **Region**: eastus
- **IaC Format**: bicep

## Layout

```
solutions/sb-microsoft-learn-ideaagent-v1/
  infra/        # Bicep / Terraform
  app/          # Minimal FastAPI scaffold + Dockerfile
.github/workflows/deploy-sb-microsoft-learn-ideaagent-v1.yml
```

## What happens on merge

Merging this PR pushes the contents to `main`, which triggers
`.github/workflows/deploy-sb-microsoft-learn-ideaagent-v1.yml`. That workflow:

1. Logs into Azure via OIDC (requires `AZURE_CLIENT_ID`, `AZURE_TENANT_ID`,
   `AZURE_SUBSCRIPTION_ID` repo secrets).
2. Creates resource group `rg-sb-microsoft-learn-ideaagent-v1-dev` in `eastus`.
3. Deploys the IaC under `solutions/sb-microsoft-learn-ideaagent-v1/infra/`.
