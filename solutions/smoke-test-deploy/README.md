# smoke-test-deploy

Auto-generated by the **Idea Generator Agent**.

- **Technique**: rag
- **Environment**: dev
- **Region**: eastus
- **IaC Format**: bicep

## Layout

```
solutions/smoke-test-deploy/
  infra/        # Bicep / Terraform
  app/          # Minimal FastAPI scaffold + Dockerfile
.github/workflows/deploy-smoke-test-deploy.yml
```

## What happens on merge

Merging this PR pushes the contents to `main`, which triggers
`.github/workflows/deploy-smoke-test-deploy.yml`. That workflow:

1. Logs into Azure via OIDC (requires `AZURE_CLIENT_ID`, `AZURE_TENANT_ID`,
   `AZURE_SUBSCRIPTION_ID` repo secrets).
2. Creates resource group `rg-smoke-test-deploy-dev` in `eastus`.
3. Deploys the IaC under `solutions/smoke-test-deploy/infra/`.
