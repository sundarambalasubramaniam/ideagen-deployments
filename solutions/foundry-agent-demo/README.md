# foundry-agent-demo

Auto-generated by the **Idea Generator Agent**.

- **Technique**: rag
- **Environment**: dev
- **Region**: eastus
- **IaC Format**: bicep

## Layout

```
solutions/foundry-agent-demo/
  infra/        # Bicep / Terraform
  app/          # Minimal FastAPI scaffold + Dockerfile
.github/workflows/deploy-foundry-agent-demo.yml
```

## What happens on merge

Merging this PR pushes the contents to `main`, which triggers
`.github/workflows/deploy-foundry-agent-demo.yml`. That workflow:

1. Logs into Azure via OIDC (requires `AZURE_CLIENT_ID`, `AZURE_TENANT_ID`,
   `AZURE_SUBSCRIPTION_ID` repo secrets).
2. Creates resource group `rg-foundry-agent-demo-dev` in `eastus`.
3. Deploys the IaC under `solutions/foundry-agent-demo/infra/`.
